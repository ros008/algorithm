#include <iostream>

using namespace std;

int main() {
	ios::sync_with_stdio(false); cin.tie(NULL);

	int T = 10;
	for (int tc = 1; tc <= T; tc++) {
		int N;
		cin >> N;

		for (int i = 0; i < N; i++) {


		}

		cout << "#" << tc << " " << "\n";
	}

	return 0;
}